# AI Call Center - Python Dependencies
# Updated with PyTorch version fix for TTS compatibility

# ============================================
# IMPORTANT: Install PyTorch FIRST with CUDA support
# ============================================
# Run this command BEFORE installing requirements.txt:
#
# For CUDA 11.8 (Recommended - works on all GPUs including RTX 3050):
#   pip install torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cu118
#
# For CUDA 12.1:
#   pip install torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cu121
#
# Then install other dependencies:
#   pip install -r requirements.txt
#
# OR use the automated fix script:
#   Windows: fix_pytorch_tts.bat
#   Linux/Mac: python fix_pytorch_tts.py
# ============================================

# LLM
google-generativeai>=0.3.0

# Speech-to-Text (Local)
openai-whisper>=20231117

# Text-to-Speech (Local)
# IMPORTANT: Requires PyTorch 2.5.1 or lower (NOT 2.9.x)
TTS==0.22.0

# Audio Processing
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.24.0

# Deep Learning
# NOTE: Install PyTorch separately with CUDA support (see instructions above)
# Do NOT uncomment these - they will install CPU-only version:
# torch>=2.0.0
# torchaudio>=2.0.0

# STT Enhancements (for better accuracy)
noisereduce>=3.0.0  # Noise reduction
librosa>=0.10.0     # Audio processing utilities
webrtcvad>=2.0.10   # Voice Activity Detection (auto-stop recording)

# Utilities
python-dotenv>=1.0.0

# Optional: For fine-tuning Whisper (advanced users)
# Uncomment if needed:
transformers>=4.30.0
datasets>=2.14.0
accelerate>=0.20.0